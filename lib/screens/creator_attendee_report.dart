import 'package:flutter/material.dart';

class CreatorAttendeeReport extends StatelessWidget {
  static const routeName = '/creatorAttendeeReport';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Attendee Report'),
      ),
      body: Container(), // This creates an empty container with no content
    );
  }
}
